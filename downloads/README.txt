Drop your built `Tabby-1.0.dmg` file here.

The download button on the homepage points to `/downloads/Tabby-1.0.dmg`.

If you rename the DMG (e.g. `Tabby-1.1.dmg` for future releases), update the `href` in `index.html`:

  <a href="downloads/Tabby-1.0.dmg" class="download" download>

You can also update the version label right after the button:

  <span class="download-meta">v1.0 · 4.2 MB · Apple Silicon &amp; Intel</span>
