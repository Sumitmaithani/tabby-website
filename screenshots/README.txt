Drop your menu bar screenshot here as `menubar.png`.

Then in `index.html`, find this block:

  <div class="screenshot">menu bar dropdown</div>

And replace it with:

  <img src="screenshots/menubar.png" alt="Tabby menu bar dropdown" class="screenshot">

Real screenshots only — no mockups.
